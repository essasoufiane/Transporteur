{% extends 'base.html.twig' %}

{% block title %}Mes commandes
{% endblock %}

{% block body %}
	<div class="video-bg">
		<video width="320" height="240" autoplay loop muted>
			<source src="https://assets.codepen.io/3364143/7btrrd.mp4" type="video/mp4">
			Your browser does not support the video tag.
		</video>
	</div>
	<div class="dark-light">
		<svg viewbox="0 0 24 24" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round" stroke-linejoin="round">
			<path d="M21 12.79A9 9 0 1111.21 3 7 7 0 0021 12.79z"/></svg>
	</div>

	{# ----header desktop--- #}
	<div class="app">
		{% include "partials/header1.html.twig" %}
		{# ----navbar left--- #}
		<div class="wrapper">
			<div class="left-side">
				<div class="side-wrapper">
					{% include "partials/navleft.html.twig" %}
				</div>
			</div>

			{# -----header mobile ---------- #}
			<main class="main-container">
				{% include "partials/headermobile.html.twig" %}

				<div class="content-wrapper">
					<div class="content-wrapper-header">

						<h1>Historique des commandes
						</h1>

					</div>

					{# formulaire inscription  #}
					<section class="content-section text-white">
						{# <div class="content-section-title">Mes commandes</div> #}
						<ul>
							<li class="adobe-product">
								<div class="products side-title">Identifiant
								</div>
								<div class="products side-title">Depart
								</div>
								<span class="status side-title">Destination
								</span>
								<span class="status side-title">Date de départ
								</span>
								<span class="status side-title desk">Date de réservation
								</span>
							</li>
							{% for order in app.user.orders %}
<a href="{{ path('app_order_show', {'id': order.id}) }}">
								<li class="adobe-product">
									<div class="products text-decoration-none side-title">{{ order.id }}
									</div>
									<div class="products text-decoration-none side-title">{{ order.depart }}
									</div>
									<span class="status text-decoration-none side-title">{{ order.destination }}
									</span>
									<span class="status text-decoration-none side-title">{{ order.dateReservation ? order.dateReservation|date('Y-m-d H:i:s') : '' }}
									</span>
									<span class="status text-decoration-none side-title desk">{{ order.dateCreation ? order.dateCreation|date('Y-m-d H:i:s') : '' }}
									</span>
									{# <span class="status"><a href="{{ path('app_order_show', {'id': order.id}) }}">voir</a>
									</span> #}

								</li></a>
							{% else %}
								<li class="adobe-product">
									<div class="products">Il n'y a aucune commade acutuellement !
									</div>

								</li>
							{% endfor %}


						</ul>
						{# fin du foàrmulaire  #}


					</section>
				</div>

				<div class="overlay-app"></div>
			</main>
		</div>
	{% endblock %}
